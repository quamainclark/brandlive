export const initialState = {
    // view: "home",
}

const reducer = (state, action) => {
    switch (action.type) {
        // case "SET_VIEW":
        //     return {
        //         ...state,
        //         view: action.view,
        //     };

        default:
            return state;
    }
}

export default reducer;